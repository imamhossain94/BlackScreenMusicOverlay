<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- OneUI -->
    <style name="OneUi.PermissionCardView.TitleStyle" parent="@android:style/Widget">
        <item name="android:textSize">17.0sp</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:fadingEdge">horizontal</item>
        <item name="android:maxLines">1</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="OneUi.BottomNavViewTextMode" parent="Widget.Design.Light.BottomNavigationView.Text" />



    <style name="AppTheme2" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary2</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark2</item>
        <item name="colorAccent">@color/colorAccent2</item>
    </style>

    <style name="AppTheme.Text">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:gravity">center_vertical|left</item>
    </style>

    <style name="AppTheme.Text.Center" parent="AppTheme.Text">
        <item name="android:gravity">center</item>
    </style>

    <style name="AppTheme.Text.Right" parent="AppTheme.Text">
        <item name="android:gravity">right|center_vertical</item>
    </style>

</resources>